предефайнед константы, которые можно переопределить
дефолтные подстановки

для работы с датой:
{{ДАТА}} - теущая дата в формате DD.MM.YYYY
{{ДАТАВРЕМЯ}} - текущая дата и время в формате DD.MM.YYYY HH:MM
{{ДД}}, {{Д}} - число, с добивкой лидирующего нуля или без
{{ММ}}, {{М}} - месяц
{{ГГГГ}}, {{ГГ}} - год

в таблице
{{rowNum}} - номер строки, так же является идентификатором по умолчанию для поиска таблиц




чтобы не ебаться с шаблонами - реализовать их обычными файлами
которые программа сама загружает

нужен парсинг шаблона для определения подстановок

так как шаблоны будут использоваться разным ПО,
шаблоны нужно разделить по приложениям

	2. наклепать веб-морду по управлению шаблонами
		которая раскладывает их по папкам
		+ показывает статистику их использования
		+ можно вести историю шаблонов

		однако, тут минус = дольше разработка

		при запросе шаблона всё равно надо указывать Application[папку]

